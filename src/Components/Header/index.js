import React from 'react';
import {Container, Logo} from './style';

const Header = props => {
    return (
        <Container primary>
            <Logo />
        </Container>
    )
}

export default Header